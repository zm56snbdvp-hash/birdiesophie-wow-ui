# Recovery backups

Use this directory only for intentional, versioned recovery packages. Never commit the entire WoW `WTF` directory.

Each archive should include:

- profile exports
- a short restore note
- SHA-256 checksum
- game/client version used for validation


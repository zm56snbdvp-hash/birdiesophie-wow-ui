# WeakAura exports

Store one sanitized export per logical HUD package.

Naming convention: `birdiesophie-wa-<package>-vMAJOR.MINOR.PATCH.txt`

Keep form-specific packages separable until the combined HUD has passed in-game QA.

